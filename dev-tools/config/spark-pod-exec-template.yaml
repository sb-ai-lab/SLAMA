---
apiVersion: v1
kind: Pod
spec:
  volumes:
    - name: scripts-volume
      configMap:
        name: exp-test-scripts
  containers:
  - name: spark-kubernetes-executor
    imagePullPolicy: Always
    volumeMounts:
      - name: scripts-volume
        mountPath: /scripts
